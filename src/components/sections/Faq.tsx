"use client";
import {useState} from "react"; import {faqItems} from "@/content/es/faq"; import {Container} from "@/components/ui/Container"; import {Section} from "@/components/ui/Section"; import {SectionHeader} from "@/components/ui/SectionHeader"; import {Icon} from "@/components/ui/Icon";
export function Faq(){const [open,setOpen]=useState<number|null>(0);return <Section id="preguntas"><Container><SectionHeader title="Preguntas frecuentes" description="Resolvemos las dudas más comunes antes de iniciar un proyecto."/><div className="mx-auto mt-10 max-w-3xl divide-y divide-[#DFE7F0] rounded-2xl border border-[#DFE7F0]">{faqItems.map(([question,answer],index)=>{const expanded=open===index;const id=`faq-answer-${index}`;return <div key={question}><h3><button type="button" className="flex w-full items-center justify-between gap-4 px-5 py-5 text-left font-bold" aria-expanded={expanded} aria-controls={id} onClick={()=>setOpen(expanded?null:index)}>{question}<Icon name={expanded?"close":"arrow"} size={20} className="shrink-0 text-[#0078B5]"/></button></h3><div id={id} hidden={!expanded} className="px-5 pb-5 leading-7 text-[#526176]">{answer}</div></div>})}</div></Container></Section>}
